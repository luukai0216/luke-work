# TronLink App · 组件 Catalog

> 来源：`design-spec.md` · 提取日期：2026-04-09
> 平台：移动端 App（iOS / Android）

---

## 使用规则

1. **颜色只使用** design-spec.md 中定义的 Token
2. 所有组件视觉按本 catalog 实现，不得自创风格
3. 生成输出须附**组件使用清单**
4. 找不到合适组件须标注 **"catalog 缺失"**

---

## 01 · Button 按钮

### 全宽按钮（Full Width · 高度 44px · 圆角 8px）

| 类型 | 背景 | 文字 | 边框 | 用途 |
| --- | --- | --- | --- | --- |
| `Primary` | `#1A212B` | `#FFFFFF` | 无 | 主要操作（确认、提交、发送） |
| `Disabled` | `#D1D3D5` | `#FFFFFF` | 无 | 条件未满足时 |
| `Secondary/Ghost` | 透明 | `#1A212B` | `1px solid ``#EBEDF0` | 次要操作（取消、跳过） |

### 半宽按钮（Half Width · 高度 48px · 宽度 162px · 圆角 8px）

| 类型 | 背景 | 文字 | 用途 |
| --- | --- | --- | --- |
| 次要（左） | `#F4F4F7` | `#1A212B` | 创建钱包 |
| 主要（右） | `#1A212B` | `#FFFFFF` | 导入钱包 |

带图标：左侧 20×20px，间距 8px

### 弹窗按钮

| 场景 | 宽度 | 高度 | 圆角 |
| --- | --- | --- | --- |
| 双按钮（取消 + 确认） | 各 145px | 40px | 8px |
| 单按钮 | 305px | 40px | 8px |

---

## 02 · Input 输入框

| 属性 | 值 |
| --- | --- |
| 高度 | 52px |
| 圆角 | 8px |
| 内边距 | 0 16px |
| 标签 | Medium 14px，`#1A212B`，margin-bottom 8px |
| 占位符 | Medium 14px，`#9BA4B6` |

| 状态 | 边框/说明 |
| --- | --- |
| 默认 | `1px solid ``#EBEDF0` |
| 聚焦 | 高亮边框 + 显示清除 ✕ |
| 错误 | `#E03B33` 边框 + 下方错误文字 |
| 禁用 | `#F4F4F7` 背景，不可交互 |

**密码输入框：** 右侧 20px 眼睛图标切换可见性

---

## 03 · 弹窗 Dialog

**双按钮弹窗：**
- 遮罩：`rgba(0,0,0,.5)`，背景 `#FFFFFF`，圆角 12px，padding 24px
- 标题：Semibold 16–18px，居中，`#1A212B`
- 正文：Regular 14px，居中，`#6D778C`
- 按钮：双按钮（取消 + 确认），各 145px，高 40px

**单按钮安全提示弹窗：**
- 样式同上，按钮 305px，高 40px

---

## 04 · 资产列表项（Asset List Item）

| 元素 | 规格 |
| --- | --- |
| 行高 | 64px |
| Token 图标 | 32×32px，border-radius 50%，带 10px 链验证徽章 |
| Token 名称 | HarmonyOS Sans SC Medium 16px，`#1A212B` |
| 余额 | Medium 16px，`#1A212B`，右对齐 |
| USD 估值 | Regular 12px，`#9BA4B6` |
| 图标与文字间距 | 12px |
| 分隔线 | 1px dashed `#F4F4F7` |

---

## 05 · 搜索栏

| 属性 | 值 |
| --- | --- |
| 高度 | 44px |
| 圆角 | 8px |
| 背景 | `#F4F4F7` |
| 图标 | 16×16px 搜索图标，左侧 padding 12px |
| 占位符 | Regular 14px，`#9BA4B6` |

---

## 06 · Switch / Toggle 开关

| 状态 | 颜色 |
| --- | --- |
| 开启 | `#1A212B` |
| 关闭 | `#D1D3D5` |

- 宽度 44px，高 24px，圆角 12px（pill 形）
- 手柄：白色圆形 20×20px，transition 200ms ease

---

## 07 · 底部弹窗 Bottom Sheet

- 从底部滑入，圆角 16px（仅顶部）
- 顶部拖拽指示条：40×4px，圆角 2px，`#D1D3D5`
- 遮罩：`rgba(0,0,0,.5)`

---

## 08 · Badge & Tag 标签

| 类型 | 背景 | 文字 | 圆角 |
| --- | --- | --- | --- |
| 状态成功 | `rgba(14,177,69,.1)` | `#0EB145` | 4px |
| 状态失败 | `rgba(224,59,51,.1)` | `#E03B33` | 4px |
| 状态待处理 | `rgba(239,134,7,.1)` | `#EF8607` | 4px |
| 网络标签 | `#CFD2FF` | `#0D1FFF` | 4px |

---

## 附：组件选择决策树

```
需要用户操作？
├── 全屏主要操作 → Button Primary 全宽 44px
├── 并排操作（创建/导入） → 半宽按钮
└── 弹窗操作 → Dialog + 双按钮 40px

需要输入？
├── 普通文字/数值 → Input 52px
├── 密码 → Input 密码（带眼睛图标）
└── 搜索过滤 → 搜索栏 44px

展示内容？
├── 资产列表 → Asset List Item 64px 行高
├── 状态标识 → Badge（成功/失败/待处理）
└── 链标识 → 网络标签

需要确认操作？
├── 简单确认 → Dialog 双按钮
└── 重要提示 → Bottom Sheet 滑入
```
