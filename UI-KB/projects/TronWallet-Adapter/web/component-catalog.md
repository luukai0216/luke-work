# TronWallet Adapter · 组件 Catalog

> 来源：`design-spec.md` + Figma 文件分析 · 提取日期：2026-04-09
> 视口：1440px Desktop | 薰衣草紫渐变背景 + 深海军蓝强调 | 开发者工具定位

---

## 使用规则

1. **颜色只使用** design-spec.md 中定义的 Token
2. 所有组件视觉按本 catalog 实现，不得自创风格
3. 生成输出须附**组件使用清单**
4. 找不到合适组件须标注 **"catalog 缺失：[描述]"**

---

## 01 · Button 按钮

### Primary Button（主按钮）

| 属性 | 值 |
|------|-----|
| 背景 | `#191A2E` |
| 文字 | `#FFFFFF`，14–16px Medium |
| 圆角 | 8px（常规）或 pill（全圆，CTA 场景） |
| Hover | 背景轻微加亮 |
| Disabled | 透明度降低 |

Navbar 右侧 CTA 规格：宽 140px，背景 `#191A2E`，白色文字，8px 圆角

### Secondary Button（次要按钮）

| 属性 | 值 |
|------|-----|
| 背景 | `#FFFFFF` |
| 文字 | `#191A2E`，14–16px Medium |
| 边框 | `1px solid #191A2E` |
| 圆角 | 8px 或 pill |
| Hover | 背景 `#F5F5F5` |

### 版本标签 Badge（特殊按钮形态）

| 属性 | 值 |
|------|-----|
| 背景 | `#A7B7FF` |
| 文字 | `#FFFFFF`，12–14px Medium |
| 圆角 | pill（全圆） |
| 内边距 | 4px 12px |

---

## 02 · Navbar 导航栏

| 属性 | 值 |
|------|-----|
| 高度 | 58px |
| 背景 | 透明或白色 |
| Logo | 图标 + 品牌名，Semi Bold，`#191A2E` |
| 导航链接 | 15–16px Regular |
| 右侧 CTA | Primary Button（140px 宽，8px 圆角） |

---

## 03 · Feature 卡片（常规）

| 属性 | 值 |
|------|-----|
| 背景 | `#FFFFFF` 或 `#F5F6FF` |
| 边框 | `1px solid #E0E3F7` |
| 圆角 | 16px |
| 内边距 | 24–32px |

**内容结构：**
1. 编号标签（`#A7B7FF` Bold，如 "01."）
2. 标题（Semi Bold，`#191A2E`）
3. 描述（Regular，`#616082`）
4. 可选：底部线条图标装饰（`#A7B7FF` 色调）

---

## 04 · 大 Feature 卡片（Hero 区块）

| 属性 | 值 |
|------|-----|
| 背景 | 多层渐变 + 装饰椭圆光晕 |
| 圆角 | 20px |
| 宽度 | 约 1200px |
| 内边距 | 48px |

**布局：** 一侧放 3D C4D 插图（蓝紫色调钱包/设备渲染图），另一侧文案 + 按钮组

---

## 05 · 代码展示卡片

| 属性 | 值 |
|------|-----|
| 背景 | `#191A2E`（深海军蓝） |
| 圆角 | 12px |
| 内边距 | 24px |

- 代码区：语法高亮（绿/白/蓝多色），等宽字体
- 可搭配功能列表（带编号圆点）

---

## 06 · 钱包 Chip

| 属性 | 值 |
|------|-----|
| 背景 | 白色半透明（frosted glass） |
| 圆角 | 12px |
| 高度 | 60px，宽度约 200px |
| 内边距 | 16px 20px |

**内容：** 钱包 Logo（32px）+ 名称（Medium，`#191A2E`）
**容器：** 深蓝渐变背景，网格排列

---

## 07 · 产品卡片

| 属性 | 值 |
|------|-----|
| 背景 | `#FFFFFF` |
| 边框 | `1px solid #E0E3F7` |
| 圆角 | 16px，高度 100px，宽约 590px |
| 内边距 | 20px 24px |

**内容：** Logo（48px 圆角）+ 名称（Semi Bold）+ 简介（Regular，`#616082`）
**右上角：** 箭头跳转按钮

---

## 08 · Footer 页脚

- 背景：`#FFFFFF` 或 `#F5F6FF`
- 内容：Logo + 文档链接 + 社交图标
- 社交图标：20px，hover → `#A7B7FF`

---

## 09 · 标题规则（双色标题）

重要标题第一个关键词用 `#A7B7FF`，其余文字 `#191A2E`。

| 层级 | 字号 | 字重 |
|------|------|------|
| Hero 大标题 | 64–72px | Bold 700 |
| Section 标题 | 36–44px | Bold 700 |
| 卡片标题 | 18–20px | SemiBold 600 |
| 编号标签 | 16px | Bold 700 |

---

## 附：组件选择决策树

```
用户行动引导？
├── 主要 CTA（下载/接入） → Primary Button（pill 或 8px 圆角）
├── 次要操作 → Secondary Button（白底深色边框）
└── 版本/功能标识 → 版本 Badge（#A7B7FF 胶囊）

展示内容卡片？
├── 功能介绍列表 → Feature 卡片（16px 圆角，编号+标题+描述）
├── 核心功能展示 → 大 Feature 卡片（20px 圆角，带 3D 插图）
├── 代码示例 → 代码展示卡片（深色 #191A2E 背景）
├── 支持的钱包 → 钱包 Chip（frosted glass，60px 高）
└── 接入的产品 → 产品卡片（白色，100px 高，带跳转箭头）

页面框架？
├── 顶部 → Navbar（58px，透明/白色背景）
└── 底部 → Footer（浅色背景）

标题文案？
└── 重要关键词用 #A7B7FF，其余 #191A2E（双色标题规则）
```
